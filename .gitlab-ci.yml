image: maven:latest

stages:
  - build
  - test
  - deploy

# Job de build
build:
  stage: build
  script:
    - mvn clean install
  artifacts:
    paths:
      - target/*.jar

# Job de tests
test:
  stage: test
  script:
    - mvn test

# Job de déploiement (optionnel)
deploy:
  stage: deploy
  script:
    - echo "Deploying the application..."

  only:
    - master
